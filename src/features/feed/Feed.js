//TODO: [] Add `<Feed>` to `<About>` 
//@flow
import * as React from 'react'

type Props = {

}

export default function Feed(props: Props) : React.Node {
    return (
        <div>
            Feed works
        </div>
    )
}